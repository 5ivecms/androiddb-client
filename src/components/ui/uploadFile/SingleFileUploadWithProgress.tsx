import { FC, useEffect } from 'react'

interface Props {
  file: File
}

const SingleFileUploadWithProgress: FC<Props> = ({ file }) => {
  useEffect(() => {}, [])

  return <div>SingleFileUploadWithProgress</div>
}

export default SingleFileUploadWithProgress
